# Hosted Team deployment removed

The centralized Hosted Team profile was removed because it violated Evolastra's local-data requirement. Do not deploy the Python API, an ingestion endpoint, or a database on a VPS.

Use the [static hosted viewer](hosted-viewer.md). Every user's Codex events, SQLite database, exports, portable analyses, and pairing capability remain on their own computer.
